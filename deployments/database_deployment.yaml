apiVersion: apps/v1
kind: Deployment
metadata:
  name: database-deployment
  namespace: web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: database
  template:
    metadata:
      labels:
        app: database
    spec:
      securityContext:
      containers:
        - name: database
          image: mysql:5.7
          ports:
            - containerPort: 3306
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            runAsNonRoot: true
            readOnlyRootFilesystem: false